import 'package:flutter/material.dart';

class AppColors {
  // BASE
  static const Color primary = Color.fromRGBO(255, 70, 0, 1); // Orange
  static const Color secondary = Color.fromRGBO(30, 52, 85, 1);
  static const Color background = Color.fromRGBO(245, 239, 220, 1); // Beige
  static const Color text = Colors.black;

  // WIDGETS
  static const Color cardBackground = Color.fromRGBO(222, 217, 200, 1); // Beige
}
